@model List<SICER.MODEL.SAPCentroCostos>

@{
    ViewBag.Title = "Listado de centros de costo";
    ViewBag.Icon = "list";

}

<div class="panel">
    <div class="panel-body">
        <br />
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <td>PrcCode</td>
                        <td>PrcName</td>
                        <td>Active</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.PrcCode</td>
                            <td>@item.PrcName</td>
                            <td>@item.Active</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
